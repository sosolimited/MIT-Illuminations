module.exports = {
    // What's the size of the canvas?
    canvas: {
        width: 1200,
        height: 100
    },
    // How many lights are we outputting to?
    lights: {
        count: 120,
    }
}
